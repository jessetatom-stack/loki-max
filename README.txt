Loki Max cache-busting build
Build: 20260320-v12

What changed:
- versioned asset URLs
- fresh service worker cache name
- network-first navigation requests
- visible build badge in header

Deploy: replace all repo files, commit, push, then use Vercel Redeploy with Build Cache OFF if needed.
